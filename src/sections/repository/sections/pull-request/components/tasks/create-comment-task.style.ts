import styled from '@emotion/styled';
import { gridSize } from '@atlaskit/theme';

export const CreateCommentTask = styled.div`
  max-width: ${gridSize() * 110}px;
`;

export const EditCommentTask = styled.div`
  max-width: ${gridSize() * 110}px;
`;

export const EditCommentFieldWrapper = styled.div`
  flex-grow: 1;
`;
