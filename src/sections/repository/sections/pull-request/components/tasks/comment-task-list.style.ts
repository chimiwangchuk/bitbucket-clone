import styled from '@emotion/styled';
import { gridSize } from '@atlaskit/theme';

export const CommentTaskList = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;
  max-width: ${gridSize() * 60}px;
`;
