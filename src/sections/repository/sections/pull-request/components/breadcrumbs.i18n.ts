import { defineMessages } from 'react-intl';

export default defineMessages({
  pullRequests: {
    id: 'frontbucket.repository.pullRequest.breadcrumbs.pullRequests',
    description: 'Text to display on pull request breadcrumbs',
    defaultMessage: 'Pull requests',
  },
});
