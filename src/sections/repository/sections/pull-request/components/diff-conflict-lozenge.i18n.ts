import { defineMessages } from 'react-intl';

export default defineMessages({
  conflictedLabel: {
    id: 'frontbucket.repository.pullRequest.diff.conflicted',
    description: 'Label for conflicted diff files',
    defaultMessage: 'CONFLICTED',
  },
});
