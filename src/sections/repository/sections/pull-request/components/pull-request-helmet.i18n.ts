import { defineMessages } from 'react-intl';

export default defineMessages({
  pullRequestTitle: {
    id: 'frontbucket.repository.pullRequest.pullRequestTitle',
    description: 'Text displayed as the pull request page title',
    defaultMessage: 'Pull Request',
  },
});
