import { defineMessages } from 'react-intl';

export default defineMessages({
  couldNotConnect: {
    id: 'frontbucket.repository.pullRequest.couldNotConnect',
    description:
      'Text for the case when user tries to approve PR but there is no connection',
    defaultMessage: 'Could not connect to server. Try again.',
  },
});
