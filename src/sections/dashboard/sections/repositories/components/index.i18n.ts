import { defineMessages } from 'react-intl';

export default defineMessages({
  pageTitle: {
    id: 'frontbucket.dashboard.repositories.pageTitle',
    description: 'Page title for the dashboard repositories page',
    defaultMessage: 'Repositories',
  },
});
