import { defineMessages } from 'react-intl';

export default defineMessages({
  isWatching: {
    id: 'frontbucket.dashboard.repositories.isWatchingFilter',
    description: 'Button name for is watching filter',
    defaultMessage: 'Watching',
  },
});
