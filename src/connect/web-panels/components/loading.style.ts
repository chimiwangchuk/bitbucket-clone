import styled from '@emotion/styled';

export const Container = styled.small`
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  margin: 0;
  padding: 0 12px;
`;

export const Spinner = styled.div`
  margin: 0 8px 0 0;
`;
