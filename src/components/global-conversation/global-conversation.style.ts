import styled from '@emotion/styled';
import { gridSize } from '@atlaskit/theme';

export const ConversationWrapper = styled.div`
  margin-bottom: ${3 * gridSize()}px;
  padding-bottom: ${3 * gridSize()}px;
`;
