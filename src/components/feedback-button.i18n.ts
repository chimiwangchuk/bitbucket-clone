import { defineMessages } from 'react-intl';

export default defineMessages({
  giveFeedback: {
    id: 'frontbucket.components.feedbackButton.giveFeedback',
    description: 'Default text for button to trigger feedback dialog',
    defaultMessage: 'Give feedback',
  },
});
