import styled from '@emotion/styled';
import GenericMessage from 'src/components/generic-message';

export const CenteredGenericMessage = styled(GenericMessage)`
  margin: auto;
  width: 100%;
  box-sizing: border-box;
`;
