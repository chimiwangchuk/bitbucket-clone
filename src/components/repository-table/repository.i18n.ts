import { defineMessages } from 'react-intl';

export default defineMessages({
  privateRepositoryIcon: {
    id: 'bitkit.repositories.repository.privateRepositoryIcon',
    description:
      "Text indicating that the repository's access level is private",
    defaultMessage: 'Private repository',
  },
});
