export const IS_ATLASKIT_ROUTER_ENABLED = 'isAtlaskitRouterEnabled';
export const IS_PARCEL_BUNDLES_ENABLED = 'isParcelBundlesEnabled';

// Deliberately calling this "active" instead of "enabled", since the
// feature can be "enabled" but the PR may not be rendered in single file mode.
export const IS_SINGLE_FILE_MODE_ACTIVE = 'isSingleFileModeActive';
export const IS_SINGLE_FILE_MODE_ELIGIBLE = 'isSingleFileModeEligible';
