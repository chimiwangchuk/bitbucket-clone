import { __extends } from "tslib";
import React from 'react';
import { PureComponent } from 'react';
import chromatism from 'chromatism';
import Color from './Color';
import { ColorPaletteWrapper } from './styles';
import { injectIntl } from 'react-intl';
import * as colors from '@atlaskit/theme/colors';
/**
 * For a given color pick the color from a list of colors with
 * the highest contrast
 *
 * @param color color string, suppports HEX, RGB, RGBA etc.
 * @return Highest contrast color in pool
 */
export function getContrastColor(color, pool) {
    return pool.sort(function (a, b) { return chromatism.difference(b, color) - chromatism.difference(a, color); })[0];
}
var ColorPalette = /** @class */ (function (_super) {
    __extends(ColorPalette, _super);
    function ColorPalette() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    ColorPalette.prototype.render = function () {
        var _a = this.props, palette = _a.palette, _b = _a.cols, cols = _b === void 0 ? 7 : _b, onClick = _a.onClick, selectedColor = _a.selectedColor, className = _a.className, formatMessage = _a.intl.formatMessage;
        return (React.createElement(ColorPaletteWrapper, { className: className, style: { maxWidth: cols * 32 } }, palette.map(function (_a) {
            var value = _a.value, label = _a.label, border = _a.border, message = _a.message;
            return (React.createElement(Color, { key: value, value: value, borderColor: border, label: message ? formatMessage(message) : label, onClick: onClick, isSelected: value === selectedColor, checkMarkColor: getContrastColor(value, [colors.N0, colors.N500]) }));
        })));
    };
    return ColorPalette;
}(PureComponent));
export default injectIntl(ColorPalette);
//# sourceMappingURL=index.js.map