import { createPlugin } from './pm-plugins/main';
var pastePlugin = function (_a) {
    var cardOptions = _a.cardOptions, sanitizePrivateContent = _a.sanitizePrivateContent;
    return ({
        name: 'paste',
        pmPlugins: function () {
            return [
                {
                    name: 'paste',
                    plugin: function (_a) {
                        var schema = _a.schema;
                        return createPlugin(schema, cardOptions, sanitizePrivateContent);
                    },
                },
            ];
        },
    });
};
export default pastePlugin;
//# sourceMappingURL=index.js.map