import { __makeTemplateObject, __read } from "tslib";
import { borderRadius, colors, themed } from '@atlaskit/theme';
import styled from 'styled-components';
export var resolveColors = function (color) {
    if (!color || color === 'grey') {
        return {
            light: [colors.N30A, colors.N800, colors.N40],
            dark: [colors.DN70, colors.DN800, colors.DN60],
        };
    }
    var anyColors = colors;
    var letter = color.toUpperCase().charAt(0);
    // NOTE: This isn't type safe. If colors change their API this may break.
    var resolvedColors = [
        anyColors[letter + "50"],
        anyColors[letter + "500"],
        anyColors[letter + "75"],
    ];
    return {
        light: resolvedColors,
        dark: resolvedColors,
    };
};
export var DateLozenge = styled.span(templateObject_1 || (templateObject_1 = __makeTemplateObject(["\n  border-radius: ", "px;\n  padding: 2px 4px;\n  margin: 0 1px;\n  position: relative;\n  transition: background 0.3s;\n  white-space: nowrap;\n  cursor: ", ";\n\n  ", ";\n"], ["\n  border-radius: ", "px;\n  padding: 2px 4px;\n  margin: 0 1px;\n  position: relative;\n  transition: background 0.3s;\n  white-space: nowrap;\n  cursor: ", ";\n\n  ",
    ";\n"])), borderRadius(), function (props) { return (props.onClick ? 'pointer' : 'unset'); }, function (props) {
    var colors = themed(resolveColors(props.color))(props);
    if (colors === '')
        colors = ['', '', ''];
    var _a = __read(colors, 3), background = _a[0], color = _a[1], hoverBackground = _a[2];
    return "\n      background: " + background + ";\n      color: " + color + ";\n      &:hover {\n        background: " + hoverBackground + ";\n      }\n    ";
});
var templateObject_1;
//# sourceMappingURL=DateLozenge.js.map