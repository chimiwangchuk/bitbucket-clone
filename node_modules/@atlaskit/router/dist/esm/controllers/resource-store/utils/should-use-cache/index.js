export var isFromSsr = function (_a) {
    var expiresAt = _a.expiresAt;
    return expiresAt === null;
};
var isFresh = function (resource) {
    if (isFromSsr(resource)) {
        return true;
    }
    return Date.now() < Number(resource.expiresAt);
};
var isCached = function (_a) {
    var data = _a.data, error = _a.error;
    return data || error;
};
export var shouldUseCache = function (resource) {
    if (resource.loading) {
        return true;
    }
    if (isCached(resource) && isFresh(resource)) {
        return true;
    }
    return false;
};
//# sourceMappingURL=index.js.map