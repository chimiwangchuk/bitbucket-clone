export var transformData = function (data, transformer) {
    return Object.keys(data).reduce(function (acc, type) {
        if (!acc[type]) {
            acc[type] = {};
        }
        Object.keys(data[type]).forEach(function (key) {
            var slice = data[type][key];
            acc[type][key] = transformer(slice);
        });
        return acc;
    }, {});
};
//# sourceMappingURL=index.js.map