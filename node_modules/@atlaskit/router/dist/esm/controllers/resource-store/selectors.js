import { __assign } from "tslib";
import { getDefaultStateSlice } from './utils';
export var getSliceForResource = function (state, props) {
    var type = props.type, key = props.key;
    var slice = state.data[type] && state.data[type][key];
    return slice ? __assign({}, slice) : getDefaultStateSlice();
};
export var getResourceStoreContext = function (state) {
    return state.context;
};
//# sourceMappingURL=selectors.js.map