import { __rest } from "tslib";
import React from 'react';
import { RouterSubscriber as BaseRouterSubscriber } from '../../router-store';
export var RouterSubscriber = function (_a) {
    var children = _a.children;
    return (React.createElement(BaseRouterSubscriber, null, function (state, _a) {
        var bootstrapStore = _a.bootstrapStore, listen = _a.listen, actions = __rest(_a, ["bootstrapStore", "listen"]);
        if (!state.unlisten && !state.isStatic) {
            listen();
        }
        return children(state, actions);
    }));
};
//# sourceMappingURL=index.js.map