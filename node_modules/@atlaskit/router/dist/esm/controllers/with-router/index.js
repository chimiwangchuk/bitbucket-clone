import { __assign } from "tslib";
import React from 'react';
import { RouterSubscriber } from '../subscribers/route';
var getWrappedComponentDisplayName = function (component) {
    var componentDisplayName = 'UNDEFINED';
    var displayName = component.displayName, name = component.name;
    if (displayName) {
        componentDisplayName = displayName;
    }
    if (name) {
        componentDisplayName = name;
    }
    return "withRouter(" + componentDisplayName + ")";
};
export var withRouter = function (WrappedComponent) {
    var displayName = getWrappedComponentDisplayName(WrappedComponent);
    var Component = WrappedComponent;
    var ComponentWithRouter = function (props) { return (React.createElement(RouterSubscriber, null, function (
    // @ts-ignore access private `history` store property
    _a, _b) {
        var route = _a.route, location = _a.location, query = _a.query, match = _a.match, action = _a.action, history = _a.history;
        var push = _b.push, replace = _b.replace;
        return (React.createElement(Component, __assign({}, props, { route: route, location: location, query: query, match: match, action: action, history: history, push: push, replace: replace })));
    })); };
    ComponentWithRouter.displayName = displayName;
    return ComponentWithRouter;
};
//# sourceMappingURL=index.js.map