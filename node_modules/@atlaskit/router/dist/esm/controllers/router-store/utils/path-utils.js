import URL from 'url-parse';
export var isAbsolutePath = function (path) {
    if (typeof path !== 'string') {
        return false;
    }
    var regex = new RegExp('^([a-z]+://|//)', 'i');
    return regex.test(path);
};
export var isExternalAbsolutePath = function (path) {
    if (typeof path !== 'string' || !isAbsolutePath(path)) {
        return false;
    }
    var pathHostname = new URL(path).hostname;
    var currentHostname = new URL(window.location.href).hostname;
    return pathHostname !== currentHostname;
};
export var getRelativePath = function (path) {
    if (typeof path !== 'string') {
        return path.pathname ? path.pathname : '';
    }
    if (!isAbsolutePath(path)) {
        return path;
    }
    var url = new URL(path);
    return "" + url.pathname + url.query + url.hash;
};
//# sourceMappingURL=path-utils.js.map