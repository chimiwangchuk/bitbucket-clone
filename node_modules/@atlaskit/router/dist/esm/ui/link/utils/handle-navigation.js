import { isKeyboardEvent, isModifiedEvent } from '../../../common/utils/event';
export var handleNavigation = function (event, _a) {
    var onClick = _a.onClick, target = _a.target, replace = _a.replace, routerActions = _a.routerActions, href = _a.href;
    if (isKeyboardEvent(event) && event.keyCode !== 13) {
        return;
    }
    onClick && onClick(event);
    if (!event.defaultPrevented && // onClick prevented default
        ((isKeyboardEvent(event) && event.keyCode === 13) || event.button === 0) && // ignore everything but left clicks and Enter key
        (!target || target === '_self') && // let browser handle "target=_blank" etc.
        !isModifiedEvent(event) // ignore clicks with modifier keys
    ) {
        event.preventDefault();
        var method = replace ? routerActions.replace : routerActions.push;
        method(href);
    }
};
//# sourceMappingURL=handle-navigation.js.map